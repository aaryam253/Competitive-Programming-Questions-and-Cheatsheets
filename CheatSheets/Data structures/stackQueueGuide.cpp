/*
Notes for stack:
    stack<int> s;

Member Functions
    s.empty()
    s.size()
    s.top()
    s.push(<value>)
    s.pop()
    s.swap(<another stack>)
	
	
Notes for queue:
    queue<int> q;

Member Functions
    q.empty()
    q.size()
    q.front()
	q.back()
    q.push(<value>)
    q.pop()
    q.swap(<another queue>)
*/